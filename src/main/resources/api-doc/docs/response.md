If an error occurs then they all are in the same format with the given error codes in the documentation.
### Example ###
```json
{
   "error": "error_code",
   "errorMessage": "human readable description"
}
```
